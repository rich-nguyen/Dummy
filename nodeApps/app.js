/*
* Module dependencies. Run "npm install" on this directory to get a node_modules dir,
* then "node app.js" to run the app.
*/

// Node's require()
var moduleA = require("./modules/moduleA")
moduleA.demoFunc()

console.log("--this App can do things--");
